[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "cd api && python init_db.py"
]

[phases.setup]
nixPkgs = ["python311"]

[start]
cmd = "cd api && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
